{
  "default_slurm_config": {
      "partition": "main",
      "cpus_per_task": 1,
      "mem": "100M"
  },
  "gpu_slurm_config": {},
  "batching_config": {
      "target_cpus_per_job": 1,
      "max_cpus_per_job": 20,
      "target_mem_per_job": 200,
      "max_mem_per_job": 32000,
      "target_num_jobs": 3,
      "max_num_jobs": 3
  }
}
